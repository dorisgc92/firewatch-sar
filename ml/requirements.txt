# Nivel 1 (WorldCover) — ligero
rasterio>=1.3
requests>=2.31

# Nivel 2 (multiespectral) — más pesado, opcional si solo quieres probar Nivel 1
torch>=2.0
torchgeo>=0.5
scikit-learn>=1.3
pystac-client>=0.7
numpy>=1.24
